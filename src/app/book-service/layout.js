export const metadata = {
  title: 'Book a Service | DevAndDone',
  description: 'Schedule a consultation with DevAndDone. Book your preferred service and time slot.',
};

export default function BookServiceLayout({ children }) {
  return children;
}

